# Recreating the ResponseKey.id bug
Clone this repository locally. Then...

## Stand up the PostgreSQL Database
```
docker-compose up
```

## Run the application
```
gradlew run
```

The original error does not appear to occur with the Kotlin annotation processors?
